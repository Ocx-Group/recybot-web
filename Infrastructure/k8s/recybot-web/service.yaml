apiVersion: v1
kind: Service
metadata:
  name: recybot-web
  namespace: ecosystem
  labels:
    app: recybot-web
spec:
  type: ClusterIP
  selector:
    app: recybot-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
